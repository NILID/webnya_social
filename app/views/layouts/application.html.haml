!!!
%html
  %head
    %title WEBNYA
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    -if current_user
      %style
        \ body {background-image: url(#{current_user.setting.bg})}
  %body
    .container
      #flash.span6.offset3= bootstrap_flash
      = yield
    #bottom
      =render 'main/panel' if current_user
